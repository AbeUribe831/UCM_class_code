#ifndef _QUEUE_H
#define _QUEUE_H
#include <string>

#include "LinkedList_char.h"

class Queue_char : public LinkedList_char
{
	public:
		
		Queue_char();
		~Queue_char();
		void enQueue_char(char value);
		char deQueue_char();
		char& front();
};
#endif
